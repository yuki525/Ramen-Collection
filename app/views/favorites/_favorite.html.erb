<h3>いいね件数: <%= post.favorites.count %></h3>
<% if current_customer.already_favorited?(post) %>
  <%= link_to 'いいねを取り消す', post_favorites_path(post), method: :delete, remote: true %>
<% else %>
  <%= link_to 'いいね', post_favorites_path(post), method: :post, remote: true %>
<% end %>
